export type CardType = {
  year: string;
  picture?: string;
  imgSrc?: string;
  author: { ru?: string; en: string };
  name: { ru?: string; en: string };
  desc?: string;
  likes?: string;
  dislikes?: string;
  watch?: string;
};
